import { SocialMedia } from "@/components/icons/social-media";
import { Menu } from "lucide-react";
import { Upload, Camera, MinusCircle } from "lucide-react";
export const Icons = {
  SocialMedia,
  Menu,
  Upload,
  Camera,
  MinusCircle,
};
